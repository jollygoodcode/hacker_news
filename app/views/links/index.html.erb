<%= link_to "submit", new_link_path %>

<ul>
<% @links.each do |link| %>
  <li id=<%= dom_id(link) %>>
    <div data-spec="score"><%= link.scores %></div>
    <%= link_to "+1", "#" %>
    <%= link_to link.title, link.url %>
  </li>
<% end %>
</ul>
